# 项目约束规则

## 1. 代码风格与规范
- 遵循项目已有的代码风格，不引入新的风格冲突
- 命名规范：
  - 变量/函数：camelCase（JS/TS）或 snake_case（Python）
  - 类/接口：PascalCase
  - 常量：UPPER_SNAKE_CASE
- 每行不超过 100-120 个字符
- 优先使用显式类型声明，避免隐式 any/unknown
- 禁止提交包含 console.log、debugger 或 TODO（未解决）的代码

## 2. 架构原则
- 遵循单一职责原则（SRP），每个模块/函数只做一件事
- 优先使用组合而非继承
- 保持依赖单向流动，禁止循环依赖
- 核心业务逻辑必须与框架无关，便于测试和迁移
- API 设计遵循 RESTful 或项目已有规范
- 数据库操作必须通过 Repository/DAO 层，禁止在业务层直接写 SQL

## 3. 安全约束
- 禁止在代码中硬编码密钥、Token、密码等敏感信息
- 所有用户输入必须经过校验和转义（XSS/SQL 注入防护）
- 接口必须做权限校验，默认拒绝未授权访问
- 敏感操作必须记录审计日志
- 文件上传限制类型和大小，禁止执行上传文件

## 4. 测试要求
- 新增功能必须包含单元测试（覆盖率目标：核心业务 ≥80%）
- 修复 Bug 必须先写复现测试，再修复代码
- 测试必须独立、可重复，不依赖外部状态
- 使用 Mock 隔离外部依赖（数据库、HTTP、文件系统等）

## 5. 错误处理
- 禁止静默吞掉异常，必须记录或抛出
- 使用项目统一的错误码体系，不随意定义新错误码
- 对外接口错误返回必须脱敏，不暴露内部堆栈
- 异步操作必须处理异常（try/catch 或 .catch）

## 6. 性能约束
- 循环中避免重复计算和频繁 DOM/IO 操作
- 大数据量操作使用分页/流式处理，禁止一次性加载全表
- 避免 N+1 查询问题，使用 JOIN 或预加载
- 缓存策略：读多写少的数据必须加缓存，同时保证缓存一致性

## 7. 依赖管理
- 引入新依赖前评估：是否必要？体积？维护活跃度？许可证？
- 优先使用项目已有依赖实现功能，避免重复造轮子
- 锁定版本号，避免自动升级导致的不兼容
- 定期审查并清理未使用的依赖

## 8. 文档与注释
- 公共 API、复杂算法、业务规则必须写注释
- 注释解释「为什么」而非「做什么」（代码本身应自解释）
- 修改代码时同步更新相关文档和注释
- README 必须包含：项目简介、安装步骤、运行方式、环境变量说明

## 9. 版本控制
- 提交信息遵循 Conventional Commits 规范
- 单次提交只做一件事，保持提交原子性
- 禁止提交包含敏感信息、大文件、构建产物
- 分支策略：main/master 保护，通过 PR/MR 合并，必须 Code Review

## 10. 环境配置
- 区分开发/测试/生产环境配置，禁止混用
- 配置通过环境变量或配置文件注入，禁止硬编码
- 提供 .env.example 模板，说明每个配置项用途